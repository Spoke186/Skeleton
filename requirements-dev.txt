-r requirements.txt

# Test, lint and type-check toolchain.
pytest==9.1.1
pytest-cov==7.1.0
hypothesis==6.165.10
ruff==0.16.3
mypy==2.3.1
pre-commit==4.6.2

# Used only by scripts/fetch_snapshot_once.py, which is run manually, once.
# See CLAUDE.md section 4: this is a fixture, not a pipeline.
yfinance==0.2.72
pyarrow==24.0.0
